{
  name = fragmentsHandler
  config {

    # tasks
    tasks {
      default {
        action = databridge
        onTransitions {
          next {
            action = te
          }
        }
      }
    }

    # actions
    actions {
      databridge {
        factory = "knot"
        config {
          address = "knotx.knot.databridge"
          deliveryOptions {
            sendTimeout = 1000
          }
        }
      }
      te {
        factory = "knot"
        config {
          address = "knotx.knot.te"
          deliveryOptions {
            sendTimeout = 1000
          }
        }
      }
    }
  }
}