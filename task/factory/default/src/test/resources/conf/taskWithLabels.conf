# node & transitions
action = a
label = a-label
onTransitions {
  _success {
    action = b
    label = b-label
    onTransitions {
      _success {
        action = c
        label = c-label
      }
    }
  }
}